<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>
<body>
  <%- include('partials/_navbar.ejs') %>

  <% if (user) { %>
    <h1>Welcome back to the CookBook, <%= user.username %>!</h1>
    <a href="/users/<%=user._id%>/foods/new">Add a new recipe</a>
    <a href="/community">View Community Pantries</a>

    <p>inviting sentence... add a create recipe to this page</p>
  <% } else { %>
    <h1>Welcome to the CookBook, guest.</h1>
    <p>
      <a href="/auth/sign-up">Sign up</a> or
      <a href="/auth/sign-in">Sign in</a>.
    </p>
  <% } %>
 
</body>
</html>
